#include <stdio.h>
int main()
{
int a,b,sum;
printf("The two numbers are");
scanf("%d%d",&a,&b);
sum=a+b;
printf("The sum of two numbers is %d",sum);
return 0;
}

 #include <stdio.h>
 int main()
 {
    int a,b,c,d,product;
    printf("The four numbers are ");
    scanf("%d%d%d%d",&a,&b,&c,&d);
    product=a*b*c*d;
    printf("The product of four numbers is %d",product);
    return 0;
 }