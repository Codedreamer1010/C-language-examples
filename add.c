#include <stdio.h>
int main()
{
    int a,b,sum;
    printf("Addition of two numbers :");
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("addition of two numbers is %d",sum);
    return 0;
}